// Este arquivo é importado automaticamente pelo vitest (conforme configurado em vitest.config.ts)
// Ele adiciona matchers customizados do jest-dom globalmente,
// facilitando asserções como expect(element).toBeInTheDocument()

import '@testing-library/jest-dom';
